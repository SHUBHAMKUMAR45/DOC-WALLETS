<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full text-white">
      <h1 class="text-3xl ml-2 px-5 py-3">ALL USERS</h1>
      <a class="text-blue-400 text-xl ml-4 px-5" href="/">Back To Home </a>
      <div class="users flex gap-4 flex-wrap mt-3">
        <% if(users.length > 0 ){ %> <% users.forEach (function(user) { %>
        <div class="w-72 bg-zinc-800 p-4 ml-4 text-xl rounded-lg">
          <div class="w-full h-52 rounded-lg bg-zinc-700 overflow-hidden">
            <img class="w-full h-full object-cover" src="<%= user.image %>" />
          </div>
          <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
          <h5 class="text-zinc-400"><%= user.email %></h5>
          <div class="flex gap-4 mt-5">
            <a class="text-zinc-300" href="/edit/<%= user._id %>">Edit Users </a>
            <a class="text-red-500" href="/delete/<%= user._id %>"
              >Delete Users
            </a>
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-white text-2xl ml-4">NO USER FOUND YET</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
